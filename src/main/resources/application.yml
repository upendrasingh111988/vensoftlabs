server:
  port: 8081
spring:
  application:
    name: vensoftlabs
  datasource:
    url: jdbc:mysql://localhost:3306/vensoftlabs
    username: root
    password: Upen@1988
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect